"""Configuration a person maintains: who gets told, and how often.

Named `settings_module` rather than `settings` because `app.config` already
exports a `Settings` class and a module of the same name two imports away is how
somebody eventually imports the wrong one.
"""
